<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>

    <title><%= data.title %></title>
    <style>
      :root {
        --main-font-size: 16px;
        --main-line-height: 1.5;
        --main-color: #fff;
        --background-color: #000;
        --nav-width: 20%;
        --nav-height: 100vh;
        --nav-background-color: rgba(36, 37, 39, 0.2);
        --content-padding: 0.9vw;
        --heading-padding: 10px;
        --h1-font-size: 2.4rem;
        --h2-font-size: 2.2rem;
        --h3-font-size: 2rem;
        --link-color: #4493f8;
      }

      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        width: 100%;
        background: var(--background-color);
        color: var(--main-color);
        font-size: var(--main-font-size);
      }

      nav {
        position: fixed;
        top: 0;
        right: 0;
        width: var(--nav-width);
        height: var(--nav-height);
        background-color: var(--nav-background-color);
      }

      #blog--content--wrapper {
        padding: var(--content-padding);
        height: 100%;
        width: calc(100% - var(--nav-width));
      }

      #blog--content--wrapper pre {
        overflow-x: auto;
        padding: 1rem;
      }

      #blog--content--wrapper h1,
      #blog--content--wrapper h2,
      #blog--content--wrapper h3,
      #blog--content--wrapper h4,
      #blog--content--wrapper h5,
      #blog--content--wrapper h6 {
        padding: var(--heading-padding);
        border-bottom: 1px solid rgba(128, 128, 128, 0.356);
      }

      #blog--content--wrapper h1 {
        font-size: var(--h1-font-size);
      }

      #blog--content--wrapper h2 {
        font-size: var(--h2-font-size);
        margin-bottom: var(--heading-padding);
      }

      #blog--content--wrapper h3 {
        font-size: var(--h3-font-size);
      }

      #blog--content--wrapper p {
        font-size: 1.1em;
        margin: 20px 0;
      }

      #blog--content--wrapper strong {
        color: rgb(158, 158, 158);
        font-weight: 600;
      }

      #blog--content--wrapper a {
        color: var(--link-color);
        text-decoration: underline;
        text-underline-offset: 0.2rem;
      }
      #blog--content--wrapper ol,
      #blog--content--wrapper ul {
        padding: 0 var(--content-padding);
      }
      #blog--content--wrapper ul {
        list-style: disc;
      }
      #blog--content--wrapper ol {
        list-style: decimal;
      }
      #blog--content--wrapper p code {
        background-color: #1e242a;
        padding: 4px 8px;
        border-radius: 20px;
      }
    </style>
  </head>
  <body>
    <div id="blog--content--wrapper">
      <h1 class="text-4xl text-center"><%= data.title %></h1>

      <figure class="max-w-screen-md mx-auto text-center">
        <svg
          class="w-10 h-10 mx-auto mb-3 text-gray-400 dark:text-gray-600"
          aria-hidden="true"
          xmlns="http://www.w3.org/2000/svg"
          fill="currentColor"
          viewBox="0 0 18 14"
        >
          <path
            d="M6 0H2a2 2 0 0 0-2 2v4a2 2 0 0 0 2 2h4v1a3 3 0 0 1-3 3H2a1 1 0 0 0 0 2h1a5.006 5.006 0 0 0 5-5V2a2 2 0 0 0-2-2Zm10 0h-4a2 2 0 0 0-2 2v4a2 2 0 0 0 2 2h4v1a3 3 0 0 1-3 3h-1a1 1 0 0 0 0 2h1a5.006 5.006 0 0 0 5-5V2a2 2 0 0 0-2-2Z"
          />
        </svg>
        <blockquote>
          <p class="text-2xl italic font-medium text-gray-900 dark:text-white">
            <%= data.description %>
          </p>
        </blockquote>
        <figcaption
          class="flex items-center justify-center mt-6 space-x-3 rtl:space-x-reverse"
        >
          <img
            class="w-6 h-6 rounded-full"
            src="https://flowbite.s3.amazonaws.com/blocks/marketing-ui/avatars/michael-gouch.png"
            alt="profile picture"
          />
          <div
            class="flex items-center divide-x-2 rtl:divide-x-reverse divide-gray-500 dark:divide-gray-700"
          >
            <cite class="pe-3 font-medium text-gray-900 dark:text-white"
              ><%= data.author %></cite
            >
            <cite class="ps-3 text-sm text-gray-500 dark:text-gray-400"
              ><%= data.date %></cite
            >
          </div>
        </figcaption>
      </figure>
    </div>
    <%- html_data %>
    <nav class="p-4">
      <h3 class="text-3xl">On This Page</h3>
      <ul>
        <% for (var heading of headingLinks) { %>
        <li class="my-2 text-lg">
          <a class="text-gray-400" href="#<%= heading.id %>"
            ><%= heading.text %></a
          >
        </li>
        <% } %>
      </ul>
    </nav>
    <script>
      document
        .querySelector("#blog--content--wrapper > p > img")
        .addEventListener("click", (e) => window.open(e.target.src));
    </script>
  </body>
</html>
